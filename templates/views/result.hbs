<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz Results</title>
  <style>
    /* Basic styles for the result page */
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f9;
      color: #333;
      padding: 20px;
    }
    h1 {
      color: #4a90e2;
    }
    .result-container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .summary {
      margin-bottom: 20px;
    }
    .summary p {
      font-size: 18px;
      margin: 5px 0;
    }
  </style>
</head>
<body>
  <div class="result-container">
    <h1>Quiz Results</h1>
    <div class="summary">
      <p><strong>Field:</strong> {{field}}</p>
      <p><strong>Correct Answers:</strong> {{correctCount}}</p>
      <p><strong>Incorrect Answers:</strong> {{incorrectCount}}</p>
      <p><strong>Result:</strong> {{#if passedCount}}Passed{{else}}Failed{{/if}}</p>
    </div>
    <div class="feedback">
      <h2>Feedback</h2>
      <ul>
        {{#each feedbackData.feedback}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
    </div>
    <div class="additional-info">
      <h2>Additional Tests</h2>
      <ul>
        {{#each feedbackData.additionalTests}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
      <h2>Additional Courses</h2>
      <ul>
        {{#each feedbackData.additionalCourses}}
        <li>{{this}}</li>
        {{/each}}
      </ul>
    </div>
    <div class="timing-info">
      <h2>Time Spent on Each Question</h2>
      <ul>
        {{#each timeLabels}}
        <li>{{this}}: {{lookup ../timeData @index}} seconds</li>
        {{/each}}
      </ul>
    </div>
  </div>
</body>
</html>
